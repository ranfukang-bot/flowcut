# 32-byte key encoded as base64. Used only to encrypt provider secrets at rest.
CREDENTIALS_MASTER_KEY=

# Optional server-side fallbacks. Keys saved in the Settings page take precedence.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
SEEDANCE_WEBHOOK_URL=
SEEDANCE_WEBHOOK_TOKEN=
