# FlowCut 客户端保护边界

## 目标

FlowCut 的安全目标不是宣称桌面软件“绝对不可逆”，而是阻止普通使用者通过解包 ASAR、修改一个授权判断或把明文工程交给 AI，短时间内得到可永久使用的完整复制品。

## v1.3.13 的保护层

1. 安装包只携带 `dist` 构建产物，不携带 `app`、`lib`、`worker`、`vendor`、数据库迁移和 TypeScript 原工程。
2. Electron 主进程、授权客户端、Gemini 调度器和 Seedance 调度器被打包并编译为 `main.jsc`。
3. Gemini preload 仅暴露白名单 IPC；选择器、上传、提交、结果提取及双轮对话逻辑被嵌入受保护主进程字节码，任务开始时由主进程注入页面。
4. Gemini 与 Seedance 执行器在每条任务开始前向 Cloudflare 申请绑定账号、设备、任务 ID 和阶段的短期签名许可。
5. 本地站点的业务 API 需要每次启动随机生成的令牌；外部进程直接访问 `127.0.0.1` 会收到 401。
6. 启动时校验受保护运行文件和站点构建产物的 SHA-256 完整性。

## 仍然不能保证的事情

- 用户电脑由用户控制，具备足够逆向经验的人仍可观察 UI、网络和内存，或重新实现相同工作流。
- 已经流出的旧版完整源码无法从对方电脑远程收回。
- 如果对方完全重写一个不访问 FlowCut 云端、直接操作自己的 Gemini 和 Seedance 账号的程序，这属于重新实现/仿制，单靠客户端授权无法阻止。
- 前端 UI 构建包和第三方开源依赖仍会随客户端存在；它们不是原始 React/TypeScript 工程，但可以被分析。

## 后续更强方案

若要进一步提高逆向成本，应把桌面主进程逐步迁移为 Rust/C++ 原生执行器，并让云端持有更多不可替代的动态编排能力。即便如此，也只能显著增加成本，不能对用户自有设备作出“绝对拿不到任何逻辑”的承诺。
