# FlowCut 自有授权中心部署与接管方案

更新时间：2026-07-30

当前状态：已部署并投入使用。

- 自有授权中心：`https://flowcut-license-control.ranfukang.workers.dev`
- 管理员入口：`https://flowcut-license-control.ranfukang.workers.dev/admin`
- 自有 D1：`flowcut-license-db`
- 桌面交付版本：`1.3.0`
- 自有授权库只保存授权账号和设备记录，不保存商品、Gem 或第三方登录数据
- 旧 ChatGPT Sites：仅保留为迁移期兼容项

## 一、最终架构

FlowCut 保持桌面端运行。每台电脑上的 Gem、商品库、商品图片、Gemini
登录态、TikTok / Seedance 登录态、任务、提示词和视频均留在本机。

Cloudflare 只运行控制中心：

- FlowCut 授权账号与密码哈希；
- 账号状态、到期时间和设备上限；
- 设备指纹哈希、设备撤销状态；
- 在线会话、离线许可证和审计记录；
- 管理员网页。
- 套餐、人工续费和收款记录；
- 正式安装版的分片云更新文件。

授权中心使用 Cloudflare Workers + D1。初期使用免费的 `workers.dev` 地址，
不需要购买传统服务器或域名。

## 二、已经完成的工作

- 独立的授权中心构建配置，不绑定商品图片使用的 R2；
- D1 授权数据库结构和可重复执行的初始化 SQL；
- 管理员、用户、设备、会话、到期时间和离线宽限；
- ES256 许可证签名和桌面端离线验签；
- 管理员登录和桌面用户登录防爆破；
- HTTPS 安全响应头及对本地业务 API 的公网隔离；
- 授权中心健康检查接口；
- 管理员授权账号 JSON 导出与空数据库导入；
- Cloudflare D1 完整 SQL 备份工具；
- 管理员密码、签名私钥和公钥的本地私密恢复资料；
- 桌面端“新授权中心为主、旧 ChatGPT Sites 为过渡备用”的配置能力；
- 普通工作台构建、自有 Cloudflare 构建和本地授权全流程测试。
- 自有 Worker 与 D1 已实际部署，线上健康检查及管理员登录已通过；
- 桌面版 `1.3.0` 已写入自有地址和公钥并完成打包；
- 已生成部署后的私密恢复包和首份 D1 SQL 备份。

## 三、你必须亲自完成的工作

以下操作涉及云账户所有权，必须由你本人完成：

1. 注册或确认一个长期由你控制的 Cloudflare 账户；
2. 绑定你长期使用的邮箱；
3. 开启 Cloudflare 两步验证并离线保存恢复码；
4. 在浏览器中完成 `wrangler login` 的登录与授权；
5. 妥善保存 `.flowcut-cloudflare` 私密恢复资料和数据库备份；
6. 如果以后购买域名，由你本人完成付款并持有域名注册账户。

不需要把 Cloudflare 密码、邮箱密码、验证码或两步验证代码告诉 AI 或开发者。

## 四、本次已经完成的迁移工作

1. 创建或复用免费的 D1 数据库；
2. 初始化授权数据表；
3. 上传管理员密码和签名密钥 Secret；
4. 构建并发布到你的 `workers.dev` 地址；
5. 执行线上健康检查和 API 隔离检查；
6. 确认新授权数据库为空，没有复制商品、Gem 或第三方登录数据；
7. 保留旧授权站和旧公钥，供现有旧版设备继续使用；
8. 把新地址和新公钥写入 FlowCut 桌面端；
9. 保留旧授权公钥作为迁移期兼容项；
10. 重新打包只依赖你自有授权中心的新安装包；
11. 完成桌面端自动化检查、安装包内部配置检查和线上管理员登录测试；
12. 生成第一份 D1 SQL 备份、新版安装包和源码接管资料。

尚未完成的可选迁移项：旧授权站已有账号导入。当前新库为 0 个账号，新的使用者
应由所有者直接在新管理员后台创建；若旧站以后存在必须保留的账号，再单独执行导出
和导入。真实干净电脑上的完整 Gemini → Seedance 成片测试仍需要接收者自己的平台
账号和网络环境。

## 五、我无法替你完成或无法保证的事情

- 不能替你成为 Cloudflare 账户、邮箱或域名的法律所有人；
- 不能绕过 Cloudflare 登录、验证码或两步验证；
- 不能在你丢失签名私钥且没有备份时恢复原许可证签名身份；
- 不能保证任何第三方免费套餐永久不调整规则；
- 不能保证桌面 EXE 绝对无法逆向，只能提高破解和滥用成本；
- 不能替你承担对外收费后的合同、隐私政策、税务和当地合规责任；
- 不能在没有真实 Gemini / Seedance 账号与额度时完成付费平台的真实生成测试。

## 六、迁移顺序

1. 当前 ChatGPT Sites 保持在线，不立即下线；
2. 在你的 Cloudflare 账户部署新的授权中心；
3. 验证管理员登录、创建测试账号、设备限制和离线许可证；
4. 从旧授权中心导出账号并导入新的空 D1；
5. 发布包含新地址、新公钥和旧站兼容项的桌面新版；
6. 通知现有使用者升级并重新登录一次；
7. 观察至少七天；
8. 确认所有用户迁移后，从后续桌面版本移除旧站公钥；
9. 保留旧站一段时间后再决定是否删除。

## 七、备份规则

必须分别保存：

- FlowCut 源码接管包；
- `.flowcut-cloudflare/recovery.json`；
- `.flowcut-cloudflare/secrets.json`；
- 最近一次 `backups/flowcut-license-*.sql`；
- Cloudflare 账户恢复码；
- 将来如有自定义域名，还要保存域名注册商的恢复资料。

签名私钥和数据库缺一不可。数据库保存“谁能用”，签名私钥证明“许可证确实
由你的授权中心签发”。
