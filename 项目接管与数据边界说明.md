# FlowCut 项目接管与数据边界说明

更新时间：2026-07-30

当前桌面交付版本：`1.3.0`

## 一、源码位置

FlowCut 主项目源码：

`C:\Users\Administrator\Desktop\全自动AI视频工作台`

FlowCut 桌面程序源码位于主项目内：

`C:\Users\Administrator\Desktop\全自动AI视频工作台\gemini-web-workbench`

当前自有 Cloudflare 授权管理站：

`https://flowcut-license-control.ranfukang.workers.dev`

管理员入口：

`https://flowcut-license-control.ranfukang.workers.dev/admin`

旧 ChatGPT Sites 授权站仅作为迁移期兼容项：

`https://flowcut-ai-video-studio.grahams-onion-5orjsy.chatgpt.site`

Sites 项目标识：

`appgprj_6a66c6befc3c8191a4dc4575dc198081`

## 二、本机业务数据的位置

本机 FlowCut 数据根目录：

`C:\Users\Administrator\AppData\Roaming\FlowCut全自动AI视频工作台`

其中主要包括：

- `site-runtime\data`：Gem、商品库、商品图片、任务、TK 归档账号名称和本机接口设置。
- `Partitions\flowcut-gemini-*`：Gemini 网页登录分区。
- `Partitions\tiktok-symphony*`：TikTok Symphony / Seedance 登录分区。
- `seedance`：内置 Seedance 队列、账号配置与任务状态。
- `workbench-state.json`：桌面工作台设置。
- `license-state.json`：当前设备的授权状态；会使用 Windows 安全存储保护会话令牌。

这些目录不在源码目录内，也不会被 Electron 安装包打包。

## 三、正常运行时的数据边界

只保存在本机、不发送给 FlowCut 公网授权站的内容：

- 用户创建或修改的 Gem；
- 商品库、商品图片和商品资料；
- Gemini 网页 Cookie 与账号登录态；
- TikTok / Seedance 网页 Cookie 与广告户登录态；
- 任务提示词、队列、生成视频和下载文件；
- TK 归档账号名称。

发送给 FlowCut 公网授权站的内容仅用于授权控制：

- FlowCut 授权账号和登录密码（通过 HTTPS 发送，服务端只保存加盐哈希）；
- 设备指纹哈希、设备名称、应用版本；
- 授权会话、到期时间、设备数量、在线检查时间与授权审计记录。
- 套餐代码、人工续费金额、续费周期和收款备注。

视频生产本身需要与第三方平台通信：

- 商品图片和提示词会按任务要求上传到 Gemini；
- 商品图片和提示词会按任务要求提交到 TikTok Symphony / Seedance；
- Google 和 TikTok 的网页登录 Cookie 只由各自的本机浏览器分区使用，不会提交给 FlowCut 授权站。

## 四、安装包的数据规则

发给其他电脑的安装包不包含本机 AppData，因此不会包含当前用户的 Gem、商品库、任务、Gemini 登录态或 TikTok / Seedance 登录态。

新设备首次安装会建立自己的空白数据目录，不会自动创建任何内置 Gem。接收者需要使用自己的账号登录，并自行创建 Gem。

升级安装不会主动删除旧设备原有的本地数据，所以在同一台电脑升级后仍能看到原来的 Gem、商品和网页登录状态。

当前桌面版为了在接收者电脑上运行本机网页服务，会随安装程序携带 FlowCut 的本机运行代码和依赖。安装包可以防止普通使用者直接改动开发目录，但不能作为源码保密或防逆向方案。若后续需要商业级代码保护，应把核心业务逻辑移到自己控制的服务端，并对桌面端做代码签名、打包收敛和必要的混淆；不要把“只有一个 EXE”理解为无法分析或修改。

## 五、公网授权站的作用和依赖

公网站只承担 FlowCut 授权账号、套餐续费、设备绑定、到期时间、离线许可证签发和桌面更新分发。公网部署启用了控制平面模式，工作台的 Gem、商品、任务、素材和执行器接口在公网均被拒绝。

套餐并发上限按每台设备独立计算。旗舰版使用 `999` 作为协议层的“不限并发”
兼容值，实际仍受本机登录账号数量、硬件性能和第三方平台能力限制。定制版把
账号自己的 `max_devices` 与 `max_concurrent` 写入授权数据库，签发许可证时
随租约下发到每一台已授权设备。

桌面端每 5 分钟尝试刷新授权。公网授权站不可用时，已经登录的设备只能在该账号设置的离线宽限期内继续运行。默认宽限期为 24 小时，可配置范围为 1 至 168 小时。

## 六、接管注意事项

自有授权中心已于 2026-07-29 部署到软件所有者本人的 Cloudflare 账户，D1
数据库、管理员密码、许可证签名密钥和首份 SQL 备份均已生成。桌面版
`1.3.0` 默认使用该自有授权中心，旧 ChatGPT Sites 只保留为旧设备迁移期兼容项。

正式安装版从 `1.3.0` 开始支持云更新。`1.2.8` 及更早版本没有更新客户端，
因此需要人工安装一次 `1.3.0`；以后版本可以在工作台内下载并重启安装。

以后接管本项目必须同时取得：

1. 本源码目录或源码接管压缩包；
2. `FlowCut授权中心私密恢复包-已部署-20260729.zip`；
3. 软件所有者本人的 Cloudflare 账户管理权；
4. 最近一次 D1 SQL 备份；
5. 主项目内的 `vendor/seedance-engine`（已随源码自包含）。

自定义域名目前不是必需项。继续使用免费 `workers.dev` 时，使用者所在网络必须
能访问该域名；如果以后购买域名，再把固定域名绑定到 Worker 并发布桌面更新。

在接管时，让新的开发者或 AI 助手先阅读本文件，再从主项目根目录检查 `git status`、`package.json`、`.openai\hosting.json`、`gemini-web-workbench\package.json` 和两个项目的 README / 测试脚本。

自有 Cloudflare 授权中心的迁移、部署和责任边界另见：

`C:\Users\Administrator\Desktop\全自动AI视频工作台\自有授权中心部署与接管方案.md`

`.flowcut-cloudflare` 是被 Git 忽略的私密恢复目录，不会进入普通源码接管包；
交接时必须由所有者单独通过安全方式提供，不能上传到公开代码仓库。普通安装包
也绝不能附带该私密恢复包。
